export const LeadEvents = {
  DELETED: 'lead.deleted',
  ASSIGNED: 'lead.assigned',
  UNASSIGNED: 'lead.unassigned',
  STATUS_CHANGED: 'lead.status.changed',
  PRIMARY_AGENT_CHANGED: 'lead.primary_agent.changed',
} as const;
